---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: minios.operator.heureso.com
spec:
  group: operator.heureso.com
  names:
    kind: Minio
    listKind: MinioList
    plural: minios
    singular: minio
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Minio is the Schema for the minios API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MinioSpec defines the desired state of Minio
            properties:
              forceRedeploy:
                description: ForceRedeploy is any string, modifying this field instructs
                  the Operator to redeploy the Operand
                type: string
              password:
                description: Password is the user password needed to login to Minio
                  UI
                type: string
              user:
                description: User is the user needed to login to Minio UI
                type: string
            required:
            - password
            - user
            type: object
          status:
            description: MinioStatus defines the observed state of Minio
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
